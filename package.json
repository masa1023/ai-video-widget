{
  "name": "ai-video-widget",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "build:widget": "pnpm --filter widget build",
    "build:admin": "pnpm --filter admin build"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}